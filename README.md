# GALLERYX — Art Gallery (artgallery.html)

## Overview
A single-page, responsive art gallery front-end showcasing curated artworks, artist profiles, buy/rent flows, and a collector dashboard. Built as a static HTML/CSS/JS prototype using Bootstrap 5 and vanilla JavaScript.

## What’s included
- `artgallery.html` — complete front-end (styles + scripts inline).
- `README.md` — this file.

## Tech
- HTML5, CSS (custom), JavaScript (vanilla)
- Bootstrap 5 (CDN)
- Bootstrap Icons (CDN)
- Images: Unsplash / pravatar placeholders

## Key details
- Client-side dataset: `artworks` array inside `artgallery.html` (JS) seeds the gallery.
- API integration: `API_BASE` is set to `http://localhost:5000` — login/signup/fetch endpoints expect a backend.
- No build tools required — purely static.

## Run locally
Option 1 — open file in browser (quick):

1. Double-click `artgallery.html` or open in your browser.

Option 2 — serve via simple HTTP server (recommended for some browser features):

PowerShell / Command Prompt:

```bash
cd "C:\Users\hp\Downloads"
python -m http.server 8000
# then open http://localhost:8000/artgallery.html
```

## Notes & Next steps
- Backend: implement an API at `http://localhost:5000` for auth and persistence.
- Accessibility: custom cursor and some interactions may affect accessibility — review ARIA roles.
- Deployment: suitable for GitHub Pages or any static host (Netlify, Vercel).

## How I can publish this for you
I can create a new GitHub repository and push these files. Options:
- Use GitHub CLI (`gh repo create <name> --public --source=. --remote=origin --push`).
- Or add a remote URL you provide (SSH or HTTPS) and push.

If you want, tell me the repository name and visibility (public/private) and whether to use `gh` or an HTTPS/SSH remote. I will create the remote and push.

---
Generated by an automated repository helper.